{
  "name": "fft-server-nodejs",
  "version": "1.0.0",
  "description": "FFT WebSocket server for vue-fft-visualizer (uses ffmpeg)",
  "main": "fft-server.js",
  "scripts": {
    "start": "node fft-server.js --device \":1\""
  },
  "dependencies": {
    "fft.js": "^4.0.4",
    "ws": "^8.16.0"
  }
}
